import { GreaterDirective } from './greater.directive';

describe('GreaterDirective', () => {
  it('should create an instance', () => {
    const directive = new GreaterDirective();
    expect(directive).toBeTruthy();
  });
});
